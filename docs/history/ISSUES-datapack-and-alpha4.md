# Archived issue history placeholder

The previous root `ISSUES.md` remains available in Git history at blob
`8851099dd55cd608ce9953dafe728cce09aaf313` and branch commit history.

This placeholder exists because the connector cannot copy an existing blob by path through the
contents API. Do not treat this file as the full archived issue log; use Git history for the verbatim
record.